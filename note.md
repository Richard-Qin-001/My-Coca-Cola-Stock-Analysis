# Coca Cola Stock Analysis
## 1.项目分析
要实现股票价格的预测，考虑使用深度学习模型。

深度学习模型的适用性： 股票价格预测本质上是一个时间序列预测问题。

循环神经网络 (RNN) 及其变体，如 长短期记忆网络 (LSTM) 和 门控循环单元 (GRU)，是处理序列数据的标准选择，因为它们能够捕捉时间序列中的长期依赖关系。

一维卷积神经网络 (1D-CNN) 也可以用于特征提取，或者与 LSTM/GRU 结合使用。

预测目标： 可以预测未来的收盘价、开盘价、最高价、最低价或简单的每日收益率。

预测限制：预测应该是短期的，因为股价可能受突发新闻影响，从而导致真实值与预测值的差值，即残差较大。

我希望使用PyTorch，但是Gemini告诉我用Tensorflow更加简单，于是决定先做一个简单模型实现。
## 2.简易版模型
训练集：全部数据的前80%
测试集：全部数据的后80%
训练效果预估：因为没有限制短时间，长期来看股价趋向于常函数，并且2010年有很多突发新闻，这些简易模型是无法获取的，也无法预测。

真实效果：
![alt text](source\image.png)

效果分析：和预期的一样，模型预期的曲线甚至可以用常函数拟合了，这样的预测是没有效果的。
## 3.重新规划
我的对上次模型训练效果差的感觉：提供的数据范围太大了，中间包含08金融危机等突发事件，如果把这一部分数据计算进去，模型最后预测的结果应该趋向于一个常函数，并且残差很大。

Gemini的分析：

您的判断非常正确。股票价格预测是一种短期时间序列预测。将从 1962 年开始的、跨越几十年的数据（其中包含 1987 年的黑色星期一、2000 年的互联网泡沫破裂、2008 年的全球金融危机、以及近期的 COVID-19 疫情等多次“结构性突变”）全部用于训练，会带来以下问题：

数据分布漂移（Data Drift）： 1960 年代的市场结构、交易量、监管环境与现在完全不同。旧数据携带的“噪音”可能超过其价值。

不稳定性： 遥远的突发事件（如 $2008$ 年金融危机）的极端值会过度影响归一化（Min-Max Scaling）的范围，导致近期更关键的价格波动在 $0$ 到 $1$ 的区间内被压缩得非常微小，模型难以学习到精细的特征。

模型惰性： 就像您所说，模型可能会为了最小化整个历史上的大残差，而趋向于预测一个平均值，从而忽略近期趋势。

为了解决这个问题，我决定将数据范围缩小到从 2025 年 1 月 1 日 开始。

选用的模型：LSTM
长短期记忆网络（Long Short-Term Memory, LSTM）

为什么选择 LSTM？

处理序列依赖关系： 股票价格数据本质上是一个序列，今天的价格与昨天、上周乃至上个月的价格都有关联。传统的全连接网络（FNN）无法有效捕捉这种时间上的依赖性。LSTM 专门设计用于处理序列数据，能够记住先前的信息。

解决梯度消失问题： 传统的 RNN 在处理长序列时，容易出现梯度消失（Vanishing Gradient）或梯度爆炸（Exploding Gradient）问题，导致模型无法学习到早期（很久以前）的数据信息。LSTM 通过其独特的“门控机制”完美地解决了这个问题。

LSTM 的核心原理（门控机制）

LSTM 的核心在于其**细胞状态（Cell State）**和三个门（Gate）：

遗忘门（Forget Gate, $f_t$）： 决定从细胞状态中丢弃哪些信息。它查看前一个隐藏状态 $h_{t-1}$ 和当前输入 $x_t$，输出一个 $0$ 到 $1$ 之间的数值向量。$1$ 表示“完全保留”， $0$ 表示“完全丢弃”。

输入门（Input Gate, $i_t$）和候选值 $\tilde{C}_t$： 决定向细胞状态中添加哪些新信息。输入门 $i_t$ 决定哪些新信息会被更新。候选值 $\tilde{C}_t$ 创建一个新的候选值向量。

细胞状态更新（$C_t$）： 这是 LSTM 的核心，它像一条传送带贯穿整个链条，信息在上面流动。新的细胞状态 $C_t$ 由旧的细胞状态 $C_{t-1}$ 经过遗忘门处理后，再加上输入门和候选值 $i_t \cdot \tilde{C}_t$ 组成。$C_t = f_t \cdot C_{t-1} + i_t \cdot \tilde{C}_t$

输出门（Output Gate, $o_t$）： 决定最终输出的隐藏状态 $h_t$。它基于新的细胞状态 $C_t$ 和输出门 $o_t$，生成新的隐藏状态 $h_t$，作为当前时间步的输出，并传递给下一个时间步。

通过这个门控结构，LSTM 能够智能地选择哪些历史信息应该被记住，哪些应该被遗忘，从而更有效地捕捉股价序列中的长期依赖关系。模型架构定义我们将使用一个标准的 多层 LSTM 架构，并在最后添加一个**全连接层（Linear Layer）**来输出预测值（即下一天的收盘价）。

``` bash
Input -> (Batch Size, Sequence Length, Input Size)
    -> LSTM Layer 1 (e.g., 128 units, return sequences=True)
    -> Dropout (防止过拟合)
    -> LSTM Layer 2 (e.g., 64 units, return sequences=False)  <- 只返回最终时间步的输出
    -> Linear Layer (Output Size: 1)
    -> Output -> (Batch Size, 1)
```
